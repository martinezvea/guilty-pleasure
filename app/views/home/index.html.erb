<h1> Products for sale </h1>

<% @products.each.do |product| %>
	<div class="well">
		<div class="row">
			<div class="col-sm-8">
				<h3><%= product.title %></h3>
			</div>
			<div class="col-sm-4">
				<div class="pull right">
				<%= form_for(@order_item) do |f| %>
		</div>
	</div>

<% end %>